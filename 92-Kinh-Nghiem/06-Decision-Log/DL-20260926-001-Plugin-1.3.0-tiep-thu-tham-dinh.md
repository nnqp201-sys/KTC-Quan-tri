# DL-20260926-001 — Plugin 1.3.0: khắc phục 4 lỗi Mức 1 trên sản phẩm (tiếp thu thẩm định độc lập lần 1, lần 2)

**Ngày:** 26/9/2026 · **Căn cứ:** 7 báo cáo thẩm định lần 1, 4 báo cáo lần 2 (ChatGPT, Grok, Gemini, Copilot) —
lần 2 ChatGPT không đồng ý đóng lỗi Mức 1 vì chưa có artifact · **Kết quả:** plugin **1.3.0**, `quan-tri` **1.13**.
Bằng chứng: `30-Ket-Qua/2026-09-26/Plugin/BANG-CHUNG-KIEM-THU-1.3.0.md` (SHA-256, danh mục tệp, log, môi trường).

## Quyết định

1. **Sửa trên sản phẩm, không chỉ trên văn bản.** Thẩm định không đóng lỗi bằng cam kết; văn bản chỉ mô tả cái đã có
   trong tệp phát hành, gắn phiên bản + SHA-256.
2. **Sao lưu GitHub ra khỏi plugin.** Hook `SessionStart` bỏ; `ktc_backup_github.py` giữ ở `29-Cong-Cu/plugin_src/scripts/`
   (Task Scheduler trỏ vào đó) nhưng không đóng gói (`CHI_DUNG_NOI_BO`). Thêm quét nội dung số định danh (CCCD) trước commit.
3. **Nhật ký mặc định chỉ thông tin mô tả** (thay phương án "trích đoạn 200 ký tự khi có tín hiệu học" của lần 1 — 3/4
   báo cáo lần 2 bác). Nội dung chỉ khi chọn: mở đầu `#học`, hoặc biến `KTC_NHAT_KY_NOI_DUNG=1` do chủ máy tự đặt.
   Xóa tệp > 30 ngày. **Hệ quả:** agent `ktc-tu-hoc` chỉ còn thấy nhãn tín hiệu, không thấy lời, trừ khi người dùng chọn.
4. **Guard `ktc_guard.py` (PreToolUse)** chặn ghi/xóa `KTC-Database`, `03-Templates`, `04-Good-Documents`; fail-closed khi
   chạy được; doctor tự thử. Giới hạn chấp nhận: không phân tích mã nhúng; thiếu Python thì hook không chạy.
5. **Chuẩn chung một bản gốc** `20-Chuan-Chung/20-Quy-Tac-Bat-Bien-Va-Khuon-Dau-Ra.md`, chèn vào 8 skill + 7 agent ở bước
   dựng — không sửa tay 8 gói (bài học: bản sao không đồng bộ sẽ lệch). 6 trạng thái, mã cảnh báo tách riêng; "cứ làm"
   khi thiếu nguồn chỉ ra bản nháp `CAN_XAC_MINH`.
6. **Lỗi YAML có sẵn** ở mô tả agent `ktc-kiem-san-pham` (": " không ngoặc, từ 19/9) — CLI 2.1.283 bắt được, bản 25/9 không.
   "Validation passed 25/9" chỉ đúng với CLI lúc đó. Bước dựng nay tự chặn.

## Kiểm chứng

`claude plugin validate --strict` đạt (2.1.283) · `kiem_tra_he_thong.py` 0 lỗi 0 cảnh báo · 20/20 bộ hồi quy ·
`test_plugin_130.py` 94 ca · SHA-256 lặp lại được qua hai lần dựng.

## Chưa làm

Nghiệm thu Chat/Cowork; rà soát 897 hai vòng cho TB v5, HD v4; kiểm kê phiên bản ở cấp tổ chức (Phòng QLKHCN&HTPT);
commit git (chờ người dùng).
