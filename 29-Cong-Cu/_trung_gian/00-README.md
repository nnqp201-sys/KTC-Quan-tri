# tools/_trung_gian — kết quả trung gian giữa các công cụ

Không phải sản phẩm, không phải mã nguồn. `trich_tuong_thuat.py` ghi ra,
`build_bc2.py` đọc vào. Xóa được bất cứ lúc nào — chạy lại là có.

Sản phẩm hoàn chỉnh đi vào `12-Output/YYYY-MM-DD/`, không để ở đây.
